- if logged_in?
  %h2.text-3xl.font-bold= "Welcome #{current_user.name}"
  - if current_user.patron?
    %p Checkout Books

  = link_to "Search Books", books_path, class: "bg-blue-400 hover:bg-blue-500 text-white font-bold py-2 px-4 rounded-full"